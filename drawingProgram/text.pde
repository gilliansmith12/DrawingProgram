//Global Variables
PFont font;

void menuButtonTextCode(String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(menuText, menuButtonX, menuButtonY, menuButtonWidth, menuButtonHeight);
  fill(white); //reset
} //End Text

void resetButtonTextCode(String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(resetText, resetButtonX, resetButtonY, resetButtonWidth, resetButtonHeight);
  fill(white); //reset
} //End Text

void saveButtonTextCode(String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(saveText, saveButtonX, saveButtonY, saveButtonWidth, saveButtonHeight);
  fill(white); //reset
} //End Text

void exitButtonTextCode(String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(exitText, exitButtonX, exitButtonY, exitButtonWidth, exitButtonHeight);
  fill(white); //reset
} //End Text

void colourButtonTextCode(String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(colourText, colourButtonX, colourButtonY, colourButtonWidth, colourButtonHeight);
  fill(white); //reset
} //End Text

void strokeButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(strokeText, strokeButtonX, strokeButtonY, strokeButtonWidth, strokeButtonHeight);
  fill(white); //reset
} //End Text

void thickButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(thickText, thickButtonX, thickButtonY, thickButtonWidth, thickButtonHeight);
  fill(white); //reset
} //End Text

void mediumButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(mediumText, mediumButtonX, mediumButtonY, mediumButtonWidth, mediumButtonHeight);
  fill(white); //reset
} //End Text

void thinButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(thinText, thinButtonX, thinButtonY, thinButtonWidth, thinButtonHeight);
  fill(white); //reset
} //End Text

void shapeButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(shapeText, shapeButtonX, shapeButtonY, shapeButtonWidth, shapeButtonHeight);
  fill(white); //reset
} //End Text

void lineButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(lineText, lineButtonX, lineButtonY, lineButtonWidth, lineButtonHeight);
  fill(white); //reset
} //End Text

void circleButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(circleText, circleButtonX, circleButtonY, circleButtonWidth, circleButtonHeight);
  fill(white); //reset
} //End Text

void squareButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(squareText, squareButtonX, squareButtonY, squareButtonWidth, squareButtonHeight);
  fill(white); //reset
} //End Text

void triangleButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(triangleText, triangleButtonX, triangleButtonY, triangleButtonWidth, triangleButtonHeight);
  fill(white); //reset
} //End Text

void eraserButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(eraserText, eraserButtonX, eraserButtonY, eraserButtonWidth, eraserButtonHeight);
  fill(white); //reset
} //End Text

void largeButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(largeText, largeButtonX, largeButtonY, largeButtonWidth, largeButtonHeight);
  fill(white); //reset
} //End Text

void medButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(medText, medButtonX, medButtonY, medButtonWidth, medButtonHeight);
  fill(white); //reset
} //End Text

void smallButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(smallText, smallButtonX, smallButtonY, smallButtonWidth, smallButtonHeight);
  fill(white); //reset
} //End Text

void backgroundButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(backgroundText, backgroundButtonX, backgroundButtonY, backgroundButtonWidth, backgroundButtonHeight);
  fill(white); //reset
} //End Text

void musicButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(musicText, musicButtonX, musicButtonY, musicButtonWidth, musicButtonHeight);
  fill(white); //reset
} //End Text

void rewindButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(rewindText, rewindButtonX, rewindButtonY, rewindButtonWidth, rewindButtonHeight);
  fill(white); //reset
} //End Text

void playButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(playText, playButtonX, playButtonY, playButtonWidth, playButtonHeight);
  fill(white); //reset
} //End Text

void pauseButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(pauseText, playButtonX, playButtonY, playButtonWidth, playButtonHeight);
  fill(white); //reset
} //End Text

void nextButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(nextText, nextButtonX, nextButtonY, nextButtonWidth, nextButtonHeight);
  fill(white); //reset
} //End Text

void skipBButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(skipBText, skipBButtonX, skipBButtonY, skipBButtonWidth, skipBButtonHeight);
  fill(white); //reset
} //End Text

void stopButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(stopText, stopButtonX, stopButtonY, stopButtonWidth, stopButtonHeight);
  fill(white); //reset
} //End Text

void skipFButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(skipFText, skipFButtonX, skipFButtonY, skipFButtonWidth, skipFButtonHeight);
  fill(white); //reset
} //End Text

void muteButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(muteText, muteButtonX, muteButtonY, muteButtonWidth, muteButtonHeight);
  fill(white); //reset
} //End Text

void unmuteButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(unmuteText, muteButtonX, muteButtonY, muteButtonWidth, muteButtonHeight);
  fill(white); //reset
} //End Text
