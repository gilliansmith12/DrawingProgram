//Global Variables
PFont font;

void quitButtonTextCode(String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, TOP); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(quitButton, rectX, rectY, rectWidth, rectHeight);
  fill(white); //reset
} //End Text

void menuButtonTextCode(String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(menuText, menuButtonX, menuButtonY, menuButtonWidth, menuButtonHeight);
  fill(white); //reset
} //End Text

void colourButtonTextCode(String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(colourText, colourButtonX, colourButtonY, colourButtonWidth, colourButtonHeight);
  fill(white); //reset
} //End Text

void strokeButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(strokeText, strokeButtonX, strokeButtonY, strokeButtonWidth, strokeButtonHeight);
  fill(white); //reset
} //End Text

void thickButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(thickText, thickButtonX, thickButtonY, thickButtonWidth, thickButtonHeight);
  fill(white); //reset
} //End Text

void mediumButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(mediumText, mediumButtonX, mediumButtonY, mediumButtonWidth, mediumButtonHeight);
  fill(white); //reset
} //End Text

void thinButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(thinText, thinButtonX, thinButtonY, thinButtonWidth, thinButtonHeight);
  fill(white); //reset
} //End Text

void shapeButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(shapeText, shapeButtonX, shapeButtonY, shapeButtonWidth, shapeButtonHeight);
  fill(white); //reset
} //End Text

void lineButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(lineText, lineButtonX, lineButtonY, lineButtonWidth, lineButtonHeight);
  fill(white); //reset
} //End Text

void circleButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(circleText, circleButtonX, circleButtonY, circleButtonWidth, circleButtonHeight);
  fill(white); //reset
} //End Text

void squareButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(squareText, squareButtonX, squareButtonY, squareButtonWidth, squareButtonHeight);
  fill(white); //reset
} //End Text

void triangleButtonTextCode (String text, int size, float rectX, float rectY, float rectWidth, float rectHeight) {
  fill(black); //Ink, hexidecimal copied from Color Selector
  textAlign (CENTER, CENTER); //Align X&Y, see Processing.org / Reference
  //Values: [LEFT | CENTER | RIGHT] & [TOP | CENTER | BOTTOM | BASELINE]
  textFont(font, size); //Change the number until it fits, largest font size
  //textFont() has option to combine font declaration with textSize()
  //Drawing Text
  text(triangleText, triangleButtonX, triangleButtonY, triangleButtonWidth, triangleButtonHeight);
  fill(white); //reset
} //End Text
